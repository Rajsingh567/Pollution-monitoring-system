{
  "name": "pollution-monitoring-system-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd pollution-monitoring-system && npm install",
    "start": "cd pollution-monitoring-system && npm start",
    "dev": "cd pollution-monitoring-system && npm run dev",
    "seed": "cd pollution-monitoring-system && npm run seed",
    "build": "cd pollution-monitoring-system && npm run build"
  },
  "dependencies": {
    "axios": "^1.15.0",
    "sqlite3": "^6.0.1"
  }
}
